export const scope = 'webinarDetail';

const message = {
  register: Object.values({
    key: `${scope}.register`,
    default: 'Register',
  }),
  ticketSummary: Object.values({
    key: `${scope}.ticketSummary`,
    default: 'Ticket Summary',
  }),
  complete: Object.values({
    key: `${scope}.complete`,
    default: 'Complete',
  }),
  shareButton: Object.values({
    key: `${scope}.shareButton`,
    default: 'Share',
  }),
  author: Object.values({
    key: `${scope}.author`,
    default: 'by',
  }),
  registerButton: Object.values({
    key: `${scope}.registerButton`,
    default: 'Register Now',
  }),
  descriptionLabel: Object.values({
    key: `${scope}.descriptionLabel`,
    default: 'Description',
  }),
  dateLabel: Object.values({
    key: `${scope}.dateLabel`,
    default: 'Date',
  }),
  timeLabel: Object.values({
    key: `${scope}.timeLabel`,
    default: 'Time',
  }),
  durationLabel: Object.values({
    key: `${scope}.durationLabel`,
    default: 'Duration',
  }),
  timezoneLabel: Object.values({
    key: `${scope}.timezoneLabel`,
    default: 'Timezone',
  }),
  ticketPriceLabel: Object.values({
    key: `${scope}.ticketPriceLabel`,
    default: 'Ticket Price',
  }),
  backButton: Object.values({
    key: `${scope}.backButton`,
    default: 'Back',
  }),
  nextButton: Object.values({
    key: `${scope}.nextButton`,
    default: 'Next',
  }),
  titleLabel: Object.values({
    key: `${scope}.titleLabel`,
    default: 'Title',
  }),
  dateTimeLabel: Object.values({
    key: `${scope}.dateTimeLabel`,
    default: 'Schedule',
  }),
  paymentButton: Object.values({
    key: `${scope}.paymentButton`,
    default: 'Payment',
  }),
  paymentMethodLabel: Object.values({
    key: `${scope}.paymentMethodLabel`,
    default: 'Payment Method',
  }),
  checkoutButton: Object.values({
    key: `${scope}.checkoutButton`,
    default: 'Checkout',
  }),
  payWithPaypal: Object.values({
    key: `${scope}.payWithPaypal`,
    default: 'Pay with Paypal',
  }),
  payWithOthers: Object.values({
    key: `${scope}.payWithOthers`,
    default: 'Other payment options coming soon!',
  }),
  ticketCompleteMessage: Object.values({
    key: `${scope}.ticketCompleteMessage`,
    default: 'Your order is complete!',
  }),
  ticketCompleteDetails: Object.values({
    key: `${scope}.ticketCompleteDetails`,
    default: 'You will be receiving an email with order details.',
  }),
  gotoDetailsButton: Object.values({
    key: `${scope}.gotoDetailsButton`,
    default: 'Go back to webinar details',
  }),
};

export default message;
